# Backend base URL the extension calls (the /api/generate endpoint).
# For production, set this to the deployed backend (e.g. the Vercel URL).
# If unset, the code falls back to http://localhost:3000 for local development.
VITE_BACKEND_BASE_URL=http://localhost:3000
